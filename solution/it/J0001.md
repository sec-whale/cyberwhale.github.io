```c++
#include <bits/stdc++.h>
#define int long long
using namespace std;
const int N=2e5+10;
struct node{
	string s; 
}p[N];
int solve(string a,string b){
	int cnt=0;
	if(a==b) return 0;
	if(a[1]!=b[0]) cnt++;
	return cnt+1;
}
signed main(){
//	freopen("string.in","r",stdin);
//	freopen("string.out","w",stdout);
    ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
	int n,sum=0;
	cin>>n;
	for(int i=1;i<=n;i++){
		cin>>p[i].s;
	}
	for(int i=1;i<=n;i++){
		if(i+1>n) sum+=solve(p[i].s.substr((p[i].s).size()-2),p[1].s.substr(0,2));
		else sum+=solve(p[i].s.substr((p[i].s).size()-2),p[i+1].s.substr(0,2));
	}
	cout<<sum<<endl;
//	fclose(stdin);
//	fclose(stdout); 
	return 0;
}
```
